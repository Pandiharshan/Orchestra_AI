// 🎨 App color palette — Aether dark-gold theme
import 'package:flutter/material.dart';

class AppColors {
  AppColors._();

  // ── Canvas & Surface ─────────────────────────────────────────────
  static const bgDeep     = Color(0xFF111318); // near-black canvas
  static const bgCard     = Color(0xFF1A1D24); // card surface
  static const bgInput    = Color(0xFF13151B); // input well
  static const googleBg   = Color(0xFF1E2029); // google button bg

  // ── Borders ──────────────────────────────────────────────────────
  static const bdNormal   = Color(0xFF2C2F3D); // default border
  static const bdFocus    = Color(0xFFD4A853); // focus / accent (warm gold)

  // ── Accent ───────────────────────────────────────────────────────
  static const accentGold  = Color(0xFFD4A853);
  static const accentGoldL = Color(0xFFF0C96A);
  static const accentGoldD = Color(0xFFB8892E);
  static const accentBtn   = Color(0xFFC49240);

  // ── Text ─────────────────────────────────────────────────────────
  static const textHigh   = Color(0xFFF2EDE6); // primary text
  static const textMid    = Color(0xFF8A8D9E); // secondary text
  static const textLow    = Color(0xFF4A4D5E); // placeholder

  // ── Semantic ─────────────────────────────────────────────────────
  static const errorRed   = Color(0xFFE05252);
  static const warnAmber  = Color(0xFFE0A030);
  static const okGreen    = Color(0xFF6FC76F);
  static const strongGreen = Color(0xFF4FC87A);

  // ── Glow ─────────────────────────────────────────────────────────
  static const glowGoldHi = Color(0x18D4A853);
  static const glowGoldLo = Color(0x0FD4A853);

  // ── Card border gradient ─────────────────────────────────────────
  static const cardBorderHi = Color(0xFF3A3D50);
  static const cardBorderLo = Color(0xFF1E2029);

  // ── Google blue ──────────────────────────────────────────────────
  static const googleBlue = Color(0xFF4285F4);

  // ── Logo inner text ──────────────────────────────────────────────
  static const logoInner  = Color(0xFF1A1200);
}
